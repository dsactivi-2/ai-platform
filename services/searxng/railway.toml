[deploy]
startCommand = """sh -c 'mkdir -p /etc/searxng && printf "use_default_settings: true\nserver:\n  secret_key: \"activi-searxng-secret-2026\"\n  limiter: false\n  image_proxy: false\n  method: \"GET\"\nsearch:\n  safe_search: 0\n  autocomplete: \"\"\n  default_lang: \"en\"\n  formats:\n    - html\n    - json\nui:\n  static_use_hash: true\n  default_theme: simple\n  center_alignment: true\n" > /etc/searxng/settings.yml && printf "[botdetection.ip_limit]\nlink_token = false\n[botdetection.ip_lists]\nblock_ip = []\npass_ip = [\n  \"0.0.0.0/0\",\n]\n" > /etc/searxng/limiter.toml && echo "[railway] Custom SearXNG config applied (limiter=false)" && exec /usr/local/searxng/entrypoint.sh'"""
